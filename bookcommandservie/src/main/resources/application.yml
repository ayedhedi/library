server:
  port: 8181

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

application:
  configurations:
    exchange: library_exchange
    saveBookKey: library.save_book_key
